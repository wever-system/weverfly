import {TextField} from '@mui/material';
import styled from '@emotion/styled';
export const STextInput = styled(({ ...props }) => <TextField {...props} size="small" sx={{backgroundColor:"#fff" }} />)`
  width: 100%;  
  `